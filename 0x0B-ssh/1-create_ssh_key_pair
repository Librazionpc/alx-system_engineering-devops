#!/usr/bin/env bash
#Scripts that creates an RSA key pair
private_key_name="school"

bits=4096

passphrase="betty"

ssh-keygen -t rsa -b "$bits" -C "Generated by 1-create_ssh_key_pair" -N "$passphrase" -f "$private_key_name"
